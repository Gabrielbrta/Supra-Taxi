import { ShortenerLongTextPipe } from './shortener-long-text-pipe';

describe('ShortenerLongTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenerLongTextPipe();
    expect(pipe).toBeTruthy();
  });
});
