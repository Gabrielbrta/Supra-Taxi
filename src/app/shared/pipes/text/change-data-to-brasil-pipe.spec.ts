import { ChangeDataToBrasilPipe } from './change-data-to-brasil-pipe';

describe('ChangeDataToBrasilPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeDataToBrasilPipe();
    expect(pipe).toBeTruthy();
  });
});
